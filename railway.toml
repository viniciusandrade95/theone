[build]
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "./.venv/bin/alembic upgrade head && ./.venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
